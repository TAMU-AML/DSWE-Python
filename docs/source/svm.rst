SVMPowerCurve
==============

Support Vector Machine (SVM) based power curve modelling.

.. code-block:: python

  from dwse import SVMPowerCurve
  model = SVMPowerCurve(X_train, y_train)
  prediction = model.predict(X_test)

.. automodule:: dswe.svm
   :members: